<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar</title>
</head>

  <body>
    <h3>Editar Desenho</h3>

    <nav>
        <ul>
            <li>
                <a href="/">PAGINA INICIAL</a>
            </li>
        </ul>
    </nav>

    <% if(message != "") { %>
    <section class="message">
        <p><%= message %></p><span id="close">&times;</span>        
    </section>
    <%} %>

    <% if(catalogo != null) { %>

    <section id="editar-desenho">   
    
        <form action = "/editar/ <%= catalogo.id %>" method ="POST">    
    
            <div>
                <label for ="nome">Digite o NOME do desenho:</label>
                <input type ="text" name ="nome" id="nome" placeholder="Digite o Nome" value="<%= catalogo.nome %>" required>
            </div>
            <div>
                <label for ="nome">Digite o ANO do desenho:</label>
                <input type ="text" name ="ano" id="ano" placeholder="Digite o Ano" value="<%= catalogo.ano %>">
            </div>
            <div>
                <label for ="nome">Digite o PERSONAGEM do desenho:</label>
                <input type ="text" name ="personagens" id="personagens" placeholder="Digite os Personagens" value="<%= catalogo.personagens %>">
            </div>
            <div>
                <label for ="nome">Digite o CRIADOR do desenho:</label>
                <input type ="text" name ="criador" id="criador" placeholder="Digite o Criador" value="<%= catalogo.criador %>">
            </div>
            <div>
                <label for ="nome">Digite a EMISSORA do desenho:</label>
                <input type ="text" name ="emissoras" id="emissoras" placeholder="Digite a Emissora" value="<%= catalogo.emissoras %>">
            </div>
            <div>
                <label for ="nome">Digite o SINOPSE do desenho:</label>
                <input type ="text" name ="sinopse" id="sinopse" placeholder="Digite a Sinopse" value="<%= catalogo.sinopse %>">
            </div>
            <div>
                <label for ="nome">Coloque endereço da IMAGEM do desenho:</label>
                <input type ="text" name ="imagem" id="imagem" placeholder="Link da Imagem" value="<%= catalogo.imagem %>">
            </div>
        
            <button type="submit">EDITAR</button>
        
        </form>
    </section>
    <%} %>

  </body>
</html>